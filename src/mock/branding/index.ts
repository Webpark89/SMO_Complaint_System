// Re-export explicitly to avoid ambiguity with initialLogoGuidelines
export {
  initialBrandingColors,
  initialLogos,
  initialBrandConfiguration,
} from "./branding.mock";
export { initialLogoGuidelines } from "./branding.mock";
